import 'package:flutter/widgets.dart';

class EditInfomationView extends StatelessWidget {
  const EditInfomationView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
